/**
 * Mobile Chain Store Hook
 * Zustand store for chain selection (mobile-specific wrapper)
 */

export {
    useAvailableChains, useChainStore,
    useCurrentChain, useSwitchChain
} from '../../packages/shared-types/chainStore';
